# PRD: GON PLAN

## 1. 개요
- 목적: 사용자 본인의 핸드폰에서만 사용하는 개인 기록/할일 관리 앱
- 배경: 해야 할 일, 하고 싶은 일, 원하는 것, 좋아하는 것을 한 곳에서 분류해 기록하고
  완료 여부를 시각적으로(취소선) 확인하고 싶음
- 대상 사용자: 본인 1인, 단일 기기(개인 스마트폰)

## 2. 목표
- 4가지 카테고리로 항목을 빠르게 기록/조회할 수 있다
- 완료한 항목은 취소선 처리되어 한눈에 구분된다
- 폰 홈 화면에 아이콘으로 설치해서 앱처럼 즉시 실행할 수 있다
- 별도 서버/로그인 없이 개인정보 걱정 없이 가볍게 쓸 수 있다

## 3. 범위
### In-scope (이번 버전)
- 4개 카테고리(해야할일/하고싶은 것/원하는 것/좋아하는 것) 탭 UI
- 카테고리별 항목 추가/수정/삭제
- 항목 완료 체크 → 취소선 스타일 적용
- LocalStorage 기반 데이터 영구 저장 (새로고침/재실행해도 유지)
- PWA 설치 지원 (manifest.json, 홈 화면 아이콘, 오프라인 기본 동작)
- 모바일 화면 최적화 (반응형이지만 사실상 모바일 전용 레이아웃)

### Out-of-scope (이번 버전 제외)
- 클라우드 동기화/백업, 다중 기기 지원
- 로그인/회원 관리
- 알림/리마인더
- 우선순위, 마감일, 태그 등 부가 속성 (향후 확장 후보)
- 다크 모드 (향후 확장 후보)

## 4. 사용자 시나리오
1. 사용자가 홈 화면 아이콘을 눌러 앱을 연다
2. 기본 탭(예: 해야할일)의 목록이 보인다
3. 하단/상단 입력창에 항목을 입력하고 추가한다
4. 항목 왼쪽 체크박스를 눌러 완료 처리 → 텍스트에 취소선 + 흐린 색 적용
5. 탭을 눌러 다른 카테고리(하고싶은 것/원하는 것/좋아하는 것)로 전환한다
6. 항목을 수정하거나 삭제할 수 있다
7. 앱을 껐다 켜도 이전 기록이 그대로 유지된다

## 5. 기능 요구사항
| ID | 기능 | 설명 |
|----|------|------|
| F1 | 카테고리 탭 | 상단에 4개 탭(해야할일/하고싶은 것/원하는 것/좋아하는 것), 탭 클릭 시 해당 목록만 표시 |
| F2 | 항목 추가 | 각 탭 내에서 텍스트 입력 후 추가 (Enter 또는 버튼) |
| F3 | 항목 완료 토글 | 체크박스 클릭 시 완료/미완료 전환, 완료 시 취소선 + 회색 처리 |
| F4 | 항목 수정 | 기존 항목 텍스트 수정 |
| F5 | 항목 삭제 | 확인 후 삭제 |
| F6 | 데이터 영속성 | LocalStorage에 카테고리별 데이터 저장/로드 |
| F7 | 정렬 | 미완료 항목이 위, 완료 항목은 아래로 자동 정렬 (확정) |
| F8 | PWA 설치 | manifest.json + 아이콘 + 기본 서비스워커로 홈 화면 설치 지원 |

## 6. 비기능 요구사항
- 모바일 브라우저(Chrome/Safari) 최신 버전에서 정상 동작
- 서버 없이 정적 파일만으로 구동 (오프라인에서도 기본 기능 사용 가능)
- 조작은 터치 친화적 크기(버튼/체크박스)로 구성
- 개인정보/외부 전송 없음 (모든 데이터는 기기 내 LocalStorage에만 저장)

## 7. 데이터 모델 (초안)
```javascript
{
  id: "1730880000000",       // 타임스탬프 기반 고유 ID
  category: "todo",           // todo | wish | want | like
  title: "우유 사기",
  memo: "",                   // 선택 사항, 향후 확장 여지
  completed: false,
  createdAt: "2026-09-01T10:00:00Z",
  completedAt: null
}
```
- 카테고리 값: `todo`(해야할일) / `wish`(하고싶은 것) / `want`(원하는 것) / `like`(좋아하는 것)
- 전체 항목은 LocalStorage에 하나의 배열(key 예: `gonPlanItems`)로 저장, category 필드로 구분

## 8. 화면 설계 개요
- 상단: 앱 타이틀(GON PLAN) + 4개 탭 바
- 탭 하단: 입력창(텍스트 입력 + 추가 버튼)
- 목록: 항목 행(체크박스 + 텍스트 + 수정/삭제 버튼), 완료 항목은 취소선 + 연한 회색으로 목록 하단에 자동 정렬
- 빈 목록일 때 안내 문구 표시

## 9. 기술 스택 (제안)
- Frontend: HTML5 + CSS3 + Vanilla JavaScript (ES6+), `bucket-list-main` 구조 참고
- 스타일: Tailwind CSS (CDN)
- 저장소: LocalStorage API
- PWA: `manifest.json`, 앱 아이콘, 최소 기능 서비스워커(오프라인 캐싱)
- 빌드 도구 없음 (정적 파일 그대로 서빙)

## 10. 프로젝트 구조 제안
```
GON_PLAN/
├── PRD.md
├── index.html
├── manifest.json
├── service-worker.js
├── icons/
│   ├── icon-192.png
│   └── icon-512.png
├── css/
│   └── styles.css
└── js/
    ├── storage.js     # LocalStorage CRUD
    └── app.js          # UI 렌더링/이벤트 처리
```
(실제 구현은 이번 문서의 범위 밖이며, 이 PRD 검토 후 별도 작업지시로 진행)

## 11. 향후 확장 후보 (이번 버전 제외)
- 클라우드 백업/동기화 (Firebase 등)
- 우선순위, 마감일, 태그
- 알림/리마인더
- 다크 모드
- 데이터 내보내기/가져오기 (JSON)
- 드래그 앤 드롭으로 순서 변경

## 12. 완료 기준 (Acceptance Criteria)
- 4개 탭이 정상적으로 전환되고 각 카테고리 데이터가 독립적으로 표시된다
- 항목 추가/수정/삭제/완료토글이 모두 정상 동작한다
- 완료 항목이 취소선으로 표시된다
- 새로고침/앱 재실행 후에도 데이터가 유지된다
- 폰 홈 화면에 아이콘으로 설치할 수 있다

## 13. PRD 확정 사항
- 완료 항목 정렬: 목록 하단으로 자동 정렬 (확정)
- 통계 기능: 미포함 (확정)
- 앱 이름: **GON PLAN** (확정)

PRD 검토가 완료되었으므로, 다음 단계는 이 문서를 기준으로 한 구현 작업지시임.
